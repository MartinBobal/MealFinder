<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MealFinder.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.MealFinder</item>
    </style>

    <style name="Theme.MealFinder" parent="Theme.Material3.DayNight.NoActionBar" />

</resources>
